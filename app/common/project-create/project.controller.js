define([], function () {
	return {
		inject: function () {
			var module = angular.module('Controllers');
			module.controller('projectCreateController', [
				'$scope',
				function ($scope) {
					
				}
			]);
		}
	};
});
