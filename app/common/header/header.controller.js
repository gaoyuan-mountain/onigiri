define([], function () {
	return {
		inject: function () {
			var module = angular.module('Controllers');
			module.controller('headerController', [
				'$scope',
				function ($scope) {
					
				}
			]);
		}
	};
});
