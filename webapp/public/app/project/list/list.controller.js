'use strict';

module.exports = {
	inject: function () {
		var module = angular.module('Controllers');
		module.controller('projectListController', [
			'$scope',
			function ($scope) {
				
			}
		]);
	}
};