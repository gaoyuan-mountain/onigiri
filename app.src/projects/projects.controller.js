define([], function () {
	return {
		inject: function () {
			var module = angular.module('Controllers');
			module.controller('projectsController', [
				'$scope',
				function ($scope) {
					
				}
			]);
		}
	};
});